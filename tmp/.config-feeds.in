	config FEED_packages
		tristate "Enable feed packages"
		depends on PER_FEED_REPO
		default y
		help
		 Enable the \"packages\" feed in opkg distfeeds.conf.
		 Say M to add the feed commented out.

	config FEED_luci
		tristate "Enable feed luci"
		depends on PER_FEED_REPO
		default y
		help
		 Enable the \"luci\" feed in opkg distfeeds.conf.
		 Say M to add the feed commented out.

	config FEED_routing
		tristate "Enable feed routing"
		depends on PER_FEED_REPO
		default y
		help
		 Enable the \"routing\" feed in opkg distfeeds.conf.
		 Say M to add the feed commented out.

	config FEED_telephony
		tristate "Enable feed telephony"
		depends on PER_FEED_REPO
		default y
		help
		 Enable the \"telephony\" feed in opkg distfeeds.conf.
		 Say M to add the feed commented out.

	config FEED_freifunk
		tristate "Enable feed freifunk"
		depends on PER_FEED_REPO
		default y
		help
		 Enable the \"freifunk\" feed in opkg distfeeds.conf.
		 Say M to add the feed commented out.

	config FEED_helloworld
		tristate "Enable feed helloworld"
		depends on PER_FEED_REPO
		default y
		help
		 Enable the \"helloworld\" feed in opkg distfeeds.conf.
		 Say M to add the feed commented out.

	config FEED_video
		tristate "Enable feed video"
		depends on PER_FEED_REPO
		default y
		help
		 Enable the \"video\" feed in opkg distfeeds.conf.
		 Say M to add the feed commented out.

	config FEED_targets
		tristate "Enable feed targets"
		depends on PER_FEED_REPO
		default y
		help
		 Enable the \"targets\" feed in opkg distfeeds.conf.
		 Say M to add the feed commented out.

	config FEED_management
		tristate "Enable feed management"
		depends on PER_FEED_REPO
		default y
		help
		 Enable the \"management\" feed in opkg distfeeds.conf.
		 Say M to add the feed commented out.

	config FEED_custom
		tristate "Enable feed custom"
		depends on PER_FEED_REPO
		default y
		help
		 Enable the \"custom\" feed in opkg distfeeds.conf.
		 Say M to add the feed commented out.

	config FEED_passwall
		tristate "Enable feed passwall"
		depends on PER_FEED_REPO
		default y
		help
		 Enable the \"passwall\" feed in opkg distfeeds.conf.
		 Say M to add the feed commented out.

	config FEED_rosy
		tristate "Enable feed rosy"
		depends on PER_FEED_REPO
		default y
		help
		 Enable the \"rosy\" feed in opkg distfeeds.conf.
		 Say M to add the feed commented out.

	config FEED_darkmatter
		tristate "Enable feed darkmatter"
		depends on PER_FEED_REPO
		default y
		help
		 Enable the \"darkmatter\" feed in opkg distfeeds.conf.
		 Say M to add the feed commented out.

